# ─── Backend ─────────────────────────────────────────────
# Real secret files — never commit these
/backend/.env
/backend/.env.development
/backend/.env.production

# Keep .env.example in git (it has no real secrets)

# Build output
/backend/dist/

# ─── Frontend ────────────────────────────────────────────
# Angular cache and build output
/frontend/.angular/
/frontend/dist/
/frontend/.vscode/

# ─── Shared ──────────────────────────────────────────────
# SQL schema (optional, uncomment if you want it ignored)
# /backend/table.sql

# Ignore all node_modules
node_modules/